<% if @page.section.blank? %>
  <h1><%=  @page.title %> </h1>
  <p> <%= raw  @page.content_for(:contenu) %></p>
<% else %>
  <%= render :partial => "section_show",  :locals => { :page => @page } %>
<% end %>
