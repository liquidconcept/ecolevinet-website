<ul id='sortable_list'>
  <%= render :partial => "page", :collection => @pages.select{|p| p.parent_id.nil? && !['/events','/news_items','/portfolio'].include?(p.link_url) }  %>
</ul>
<%= render :partial => "/shared/admin/sortable_list",
           :locals => {:continue_reordering => !!local_assigns[:continue_reordering]} %>
