<ul>
  <% if Admin::newsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_news_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_news_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::newsController.sortable? and news.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "news"),
                 admin_news_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "news"),
                 admin_news_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
